# TTJetMassCombination

This repo centralizes all inputs, the combination setup, plotting and fitting scripts, and the results.

## Inputs
The theoretical combinations are stored in `powheg_generations/` and `powheg_generations_8tev/` respectively. They are synched from `/eos` and can be downloaded via `downloadTheory.sh`.
The files are managed via `git -lfs`.